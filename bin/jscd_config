SRC_DIR='tests/clone/jquery'

DECKARD_DIR="$SAFE_HOME/lib/deckard"

MIN_TOKENS='50 100'

STRIDE='2'

SIMILARITY='0.99 1.0'

VECTOR_DIR="$SAFE_HOME/build/vectors"

CLUSTER_DIR="$SAFE_HOME/build/clusters"

TIME_DIR="$SAFE_HOME/build/times"

GROUPING_EXEC="$DECKARD_DIR/src/vgen/vgrouping/runvectorsort"

CLUSTER_EXEC="$DECKARD_DIR/src/lsh/bin/enumBuckets"

POSTPRO_EXEC="$DECKARD_DIR/scripts/clonedetect/post_process_groupfile"

GROUPING_S='50'
