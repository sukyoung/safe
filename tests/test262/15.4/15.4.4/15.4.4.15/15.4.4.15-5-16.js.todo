  function testcase() 
  {
    var arr = [];
    arr[Math.pow(2, 32) - 2] = true;
    return arr.lastIndexOf(true, "Infinity") === (Math.pow(2, 32) - 2);
  }
  {
    var __result1 = testcase();
    var __expect1 = true;
  }
  