  function testcase() 
  {
    var fromIndex = {
      valueOf : (function () 
      {
        return 2;
      })
    };
    var targetObj = (function () 
    {
      
    });
    return [0, true, targetObj, 3, false, ].lastIndexOf(targetObj, fromIndex) === 2 && [0, true, 3, targetObj, false, ].lastIndexOf(targetObj, fromIndex) === - 1;
  }
  {
    var __result1 = testcase();
    var __expect1 = true;
  }
  