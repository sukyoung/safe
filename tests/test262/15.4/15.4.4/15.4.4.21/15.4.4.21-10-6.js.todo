  function testcase() 
  {
    foo.prototype = [1, 2, 3, 4, ];
    function foo() 
    {
      
    }
    var f = new foo();
    function cb(prevVal, curVal, idx, obj) 
    {
      return prevVal + curVal;
    }
    if (f.reduce(cb, - 1) === 9)
      return true;
  }
  {
    var __result1 = testcase();
    var __expect1 = true;
  }
  