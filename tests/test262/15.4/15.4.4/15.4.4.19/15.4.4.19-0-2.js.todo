// TODO Array.prototype.map.length
  function testcase() 
  {
    if (Array.prototype.map.length === 1)
    {
      return true;
    }
  }
  {
    var __result1 = testcase();
    var __expect1 = true;
  }
  
