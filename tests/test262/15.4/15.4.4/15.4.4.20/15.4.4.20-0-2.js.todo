// TODO Array.prototype.filter.length
  function testcase() 
  {
    if (Array.prototype.filter.length === 1)
    {
      return true;
    }
  }
  {
    var __result1 = testcase();
    var __expect1 = true;
  }
  
