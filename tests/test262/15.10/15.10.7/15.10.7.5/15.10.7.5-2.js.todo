  function testcase() 
  {
    var d = Object.getOwnPropertyDescriptor(RegExp.prototype, 'lastIndex');
    if (d.writable === true && d.enumerable === false && d.configurable === false)
    {
      return true;
    }
  }
  {
    var __result1 = testcase();
    var __expect1 = true;
  }
  