  function testcase() 
  {
    var desc = Object.getOwnPropertyDescriptor(Function, "length");
    if (desc.value === 1 && desc.writable === false && desc.enumerable === false && desc.configurable === false)
      return true;
  }
  {
    var __result1 = testcase();
    var __expect1 = true;
  }
  