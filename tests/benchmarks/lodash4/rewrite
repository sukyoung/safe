#!/bin/sh

for file in tests/*; do
  if [ -f "$file" ]; then
    base=`basename "$file"`
    to="tests-rewritten/$base"
    echo "Rewriting $file to $to..."
    node ../../../dynamic-shortcut/rewrite.js $file $to
  fi
done
