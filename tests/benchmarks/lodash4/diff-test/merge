#!/bin/sh

cd $SAFE_HOME/tests/benchmarks/lodash4

TARGET="diff-test/merged.js"

cat setting.js lodash.js test-setting.js > $TARGET
if [ "$#" -eq 1 ]; then
  cat $1 >> $TARGET
fi
node $DS_HOME/parenthesize.js $TARGET $TARGET
