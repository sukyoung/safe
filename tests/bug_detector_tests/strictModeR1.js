"use strict";

// [R1]
var implements, interface, let, package, private, protected, public, static, yield;

function notStrict() {
    // [R1]
    var implements, interface, let, package, private, protected, public, static, yield;
}

notStrict();
